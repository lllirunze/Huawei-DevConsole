import React from "react";

export default function MR(): JSX.Element {
    return (
        <section>
            <h1>MR Panel</h1>
            <p>此处将实现向 CodeHub 提交代码、创建 MR、为 MR 加分等功能。</p>
        </section>
    );
}
