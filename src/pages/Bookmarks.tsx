import React from "react";

export default function Bookmarks(): JSX.Element {
    return (
        <section>
            <h1>Bookmarks</h1>
            <p>此处将展示来自 Windows Chrome 的书签（稍后接入）。</p>
        </section>
    );
}
